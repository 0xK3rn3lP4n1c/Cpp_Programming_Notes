#include <iostream>
using namespace std;


void age (int x) { // Here with the parameters you can define the type of the variable it needs to work. For example our function here needs a number 
	cout<<"I am "<<x<<" years old"; 
}
int main () {
	age(22); //As you can see with this function when calling the function you can give the variable in the paranthesis
	return 0;
}
